<UserControl x:Class="DDOLayoutEditor.ItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DDOLayoutEditor"
             mc:Ignorable="d" 
             d:DesignHeight="304" d:DesignWidth="286">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Canvas Name="MainCanvas" Background="Blue" Opacity="0.5"></Canvas>
        <Viewbox Name="MainViewbox" IsHitTestVisible="False" StretchDirection="DownOnly">
            <TextBlock Name="MainText" Foreground="Black" FontSize="18" TextWrapping="NoWrap" Text="ItemControl" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="8" ></TextBlock>
        </Viewbox>
        <Rectangle Name="MainRect" Fill="Transparent" Stroke="Red" StrokeThickness="1" StrokeDashArray="1"/>
    </Grid>
    <UserControl.ToolTip>
        <ToolTip x:Name="MainToolTip"></ToolTip>
    </UserControl.ToolTip>
</UserControl>
